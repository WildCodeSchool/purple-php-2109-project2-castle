{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Hello Wilder !</h1>
    
    {{ dump(enemy) }}
    {{ dump(archer) }}
    {{ dump(pikeman) }}
    {{ dump(knight) }}

 
 


{% endblock %}